version: '3.8'

services:
  api:
    build: .
    container_name: ecotracker-ml-api
    ports:
      - "8000:8000"
    environment:
      - HOST=0.0.0.0
      - PORT=8000
      - DEBUG=False
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    restart: unless-stopped
